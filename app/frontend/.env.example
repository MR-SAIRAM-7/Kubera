# Backend origin for API and WebSocket connections.
# Local development:
VITE_BACKEND_URL=http://localhost:8000

# Production option (same-domain backend behind reverse proxy):
# VITE_BACKEND_URL=https://your-domain.com
